# 📦 GitHub へのアップロードと EXE 入手の完全ガイド

初めての方でも 100% 迷わずに `ScreenMind.exe` を手に入れるための手順書です。

## ステップ 1：GitHub で新しい場所を作る
1. [GitHub](https://github.com/) にログインします。
2. 画面右上の **[+]** アイコンをクリックし、**[New repository]** を選択します。
3. **Repository name** に `my-screenmind` と入力します。
4. 一番下の **[Create repository]** をクリックします。

## ステップ 2：ファイルをアップロードする
1. 作成後の画面にある **「uploading an existing file」** という青いリンクをクリックします。
2. 私がお送りした ZIP を解凍して出てきた**すべてのファイルとフォルダ**を、画面のグレーの枠内にドラッグ＆ドロップします。
   *   **注意:** `.github` フォルダが重要です。これがないと EXE が作られません。
3. 下にある **[Commit changes]** ボタンをクリックします。

## ステップ 3：自動ビルドを確認する
1. 画面上部のメニューにある **[Actions]** タブをクリックします。
2. `ScreenMind Continuous Build` という項目が表示され、黄色い丸（作業中）が回っているのを確認します。
3. 約 3〜5 分待ちます。緑色のチェックマーク（✅）に変われば成功です！

## ステップ 4：完成した EXE をダウンロードする
1. 一番上の履歴（`ScreenMind Continuous Build`）をクリックします。
2. 画面を一番下までスクロールすると **「Artifacts」** という項目があります。
3. **`ScreenMind-Windows`** という文字をクリックしてダウンロードします。
4. ダウンロードした ZIP を解凍すると、中に **`ScreenMind.exe`** が入っています！

---

## ⚠️ もし「依存ライブラリが〜」と表示される場合
今回のバージョン（v4.1.1）では、起動時に**自動診断**を行う機能を追加しました。
1. `ScreenMind.exe` を実行した際に出る**黒い画面の文字**をすべてコピー、またはスクリーンショットを撮ってください。
2. その内容を教えていただければ、どのライブラリが不足しているか1秒で特定し、即座に修正できます。
3. ビルドが成功していれば、黒い画面に「✅ すべての依存関係が正常です」と表示され、アプリが起動します。
