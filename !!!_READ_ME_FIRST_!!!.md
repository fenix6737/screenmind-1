# ⚠️ 重要：ScreenMind.exe を手に入れる方法

このパッケージをダウンロードしただけでは、まだ `ScreenMind.exe` は存在しません。
**GitHub を使って、あなた専用の実行ファイル（EXE）を自動生成する必要があります。**

## 🚀 EXE を手に入れる 3 ステップ

### 1. GitHub にこのフォルダをアップロードする
GitHub で新しいリポジトリ（名前は何でも OK）を作成し、このフォルダの中身をすべてアップロード（Push）してください。

### 2. 自動ビルドを待つ（約 3〜5 分）
アップロードすると、GitHub が自動的に Windows 用の EXE や Mac 用の APP を作り始めます。
*   GitHub の画面上部にある **「Actions」** タブをクリックすると、進捗が見れます。

### 3. 完成したファイルをダウンロードする
ビルドが終わると、その詳細画面の下の方にある **「Artifacts」** という場所に、完成したファイルが表示されます。
*   `ScreenMind-Windows` → Windows 用の `ScreenMind.exe` が入っています。
*   `ScreenMind-macOS` → Mac 用の `ScreenMind.app` が入っています。

---

## ❓ なぜ最初から入っていないのですか？
セキュリティと環境の制限により、AI が直接 Windows 用の実行ファイルを作成して配布することができないためです。GitHub の公式なビルド機能を使うことで、**「安全で、あなたの PC で確実に動く最新の EXE」** を手に入れることができます。

詳細は `GITHUB_DEPLOY_GUIDE.md` をご覧ください。
